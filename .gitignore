# Build outputs
.gradle/
build/
app/build/
*/build/

# Local config
local.properties

# Signing keystore — keep this PRIVATE, do not commit
app/release.keystore

# IDE
.idea/
*.iml
.DS_Store

# Misc / temp
build_out.txt
*.log
_updatetest/
u*.xml
pulled_cache.m3u8

# Release APK artifacts (attach to GitHub Releases, don't commit)
Tarango-v*.apk

# Source art (already processed into app/src/main/res) and personal notes
art/
info.txt
